SELECT
    CONCAT('฿ ', FORMAT(SUM(o.amount), 0)) AS totalRevenue,
    FORMAT(COUNT(o.order_id), 0) AS totalTransactions
FROM 
    mpete_vending.orders o
LEFT JOIN 
    mpete_vending.shops s ON o.shop_id = s.shop_key
WHERE 
    o.status IN ('paid', 'late_paid');